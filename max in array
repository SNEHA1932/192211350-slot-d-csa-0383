#include<stdio.h>
int main()
{
	int size,i,max=0,a[50];
	scanf("%d",&size);
	for(i=0;i<size;i++)
	{
		scanf("%d",&a[i]);
	}
	max=max+a[0];
	for(i=0;i<size;i++)
	{
		if(max<a[i])
		{
			max=a[i];
		}
		
}
	printf("%d is maximum",max);
	

	return 0;
